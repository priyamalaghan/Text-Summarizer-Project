from pathlib import Path

#This creates a Path object that points to a file called config.yaml located inside the config/ folder
CONFIG_FILE_PATH = Path("config/config.yaml")
#This defines another Path object pointing to a file called params.yaml, located in the root directory of the project.
PARAMS_FILE_PATH = Path("params.yaml")
